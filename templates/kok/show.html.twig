{% extends 'base.html.twig' %}

{% block title %}Kok{% endblock %}

{% block body %}
    <h1>Kok</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kok.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('kok_index') }}">back to list</a>

    <a href="{{ path('kok_edit', {'id': kok.id}) }}">edit</a>

    {{ include('kok/_delete_form.html.twig') }}
{% endblock %}
